package webserver.common.constant;

import webserver.servlet.BaseServlet;

import java.util.Optional;

public class HttpServer {
//    public static final String WEB_ROOT;
//
//   static {
//        WEB_ROOT = Optional.ofNullable(HttpServer.class.getClassLoader().getResource("webserver"))
//                .orElseThrow(() -> new IllegalStateException("can't not find user web root file."))
//                .getFile().substring(1);
//    }
}
